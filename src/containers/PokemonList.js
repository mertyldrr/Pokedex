import React from 'react';
import { Button } from "react-bootstrap";

const PokemonList = () => {
    return (
        <div>
            <Button variant="danger">Pokemon List</Button>
        </div>

    )
};

export default PokemonList;